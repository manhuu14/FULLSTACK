/**
 * works done in js
 * console.dir(document) .first one  
 * inside that expand all  we see html elements  
 * inside that when we open h1 or tags we see classname is important and id and innertext and innerhtml all visible when you click tag  
 * console.dir(docuemnt.all);
 * we can print like this also 
 * console.dir(document.all[0]);
 * console.dir(document.all[0].innerText="peter parker");
 */   
//selecting elements 
//document.getElementById(mainImg); we can get that id mentioned tag information 
//we can asses this type also 
//let imgobj=document.getElementById("mainImg"); 
//console.log(imgObj);
