let dice=Math.floor(Math.random()*6)+1; 
console.log(dice);
let car={
    name:"benz",
    model:"54x",
    color:"olive green"
} 
console.log(car.name); 
let person={
    name:'alexender',
    age:'30',
    city:'vvv'
}