function sum(a=2,b){
    return a+b;
}
sum(1,3);//4
sum(1);// a=1 b=undefined beacuse in js they cant assume a is defined we want to define it to b 
//js knows only one thing first one to a and second parameter to b thats only
