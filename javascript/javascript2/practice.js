let nam=prompt("enter your name");
let age=prompt("enter your age");
console.log(`name of the person is${nam} and the age of the person is ${age}`) 
//find the smallest on among the given ones let we know how to do it easily 

let a=4532;
let b=12; 
let f=22;
for(let i=0;i<a.length;i++){ 
    let d=a[i];
    for(let c=0;c<b.length;b++){
        if()
    }
}